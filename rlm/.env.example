# LLM proxy credentials (required).
LITELLM_BASE_URL=https://litellm.inference.cool/v1
LITELLM_API_KEY=sk-...

# HTTP Basic Auth gate for the web UI (optional). When set, the FastAPI
# server requires these credentials on every request.
HALO_AUTH_USER=view
HALO_AUTH_PASS=view2026
