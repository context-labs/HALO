# Required for the OpenAI Agents SDK to call the model.
OPENAI_API_KEY=

# Optional — where to write the JSONL.gz trace file. Defaults to
# ./traces.jsonl.gz relative to the working directory.
# HALO_TRACES_PATH=./traces.jsonl.gz
